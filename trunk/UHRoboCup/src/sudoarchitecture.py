import config
import threading
#over is the variable that makes it keep playing the game until it is told games over
over =0;
statein = 0;
class stateone(threading.Thread):
    def __init__(self):

while over == 0:
    if statein == 0:
        state.initial()
